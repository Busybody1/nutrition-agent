# =============================================================================
# NUTRITION AI AGENT ENVIRONMENT CONFIGURATION
# =============================================================================

# REQUIRED ENVIRONMENT VARIABLES
# =============================================================================

# Main Database Connection (PostgreSQL)
DATABASE_URL=postgresql://username:password@host:port/database_name

# AI Service API Key (Groq)
GROQ_API_KEY=your_groq_api_key_here

# =============================================================================
# OPTIONAL BUT RECOMMENDED ENVIRONMENT VARIABLES
# =============================================================================

# Separate Nutrition Database (if using multi-database setup)
NUTRITION_DB_URI=postgresql://username:password@host:port/nutrition_db

# Redis for Caching and Session Management
REDIS_URL=redis://host:port

# Service Configuration
ENVIRONMENT=development
DEBUG=true

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# =============================================================================
# ADVANCED CONFIGURATION (Optional)
# =============================================================================

# Groq API Configuration
GROQ_BASE_URL=https://api.groq.com/openai/v1
GROQ_MODEL=llama3-70b-8192

# Database Connection Pooling
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Cache TTL Settings (seconds)
CACHE_TTL_SHORT=300
CACHE_TTL_MEDIUM=3600
CACHE_TTL_LONG=86400

# Service Ports
NUTRITION_AGENT_PORT=8001

# Request Timeouts
REQUEST_TIMEOUT_SECONDS=30
LLM_TIMEOUT_SECONDS=60

# Security Settings
JWT_SECRET_KEY=your_jwt_secret_key_here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=100
RATE_LIMIT_REQUESTS_PER_HOUR=1000

# Monitoring
MONITORING_ENABLE_METRICS=true
MONITORING_METRICS_PORT=9090

# =============================================================================
# NOTES
# =============================================================================

# 1. DATABASE_URL is REQUIRED for the agent to function
# 2. GROQ_API_KEY is REQUIRED for AI meal planning features
# 3. REDIS_URL is recommended for production (session management and caching)
# 4. NUTRITION_DB_URI is optional - will fall back to main database if not provided
# 5. All other variables have sensible defaults and are optional
# 6. For Heroku deployment, set DATABASE_URL and REDIS_URL from add-ons
# 7. For local development, you can use SQLite by setting DATABASE_URL=sqlite:///./nutrition.db
